# v0.1.1 Runtime Hardened

## What was completed
- Added backend dependency declarations for SQLAlchemy and Alembic
- Initialized Alembic scaffolding
- Added seed entrypoint placeholder
- Added frontend typecheck support
- Added tsconfig.json
- Replaced obsolete JSX entrypoint with TypeScript entrypoint
- Verified local API health endpoint
- Verified frontend build
- Verified Docker build and runtime
- Added root .gitignore
- Tagged release as v0.1.1-runtime-hardened

## What is still incomplete
- Alembic is initialized, but no real application migration has been authored yet
- Seed script is still a placeholder, not real demo/bootstrap data
- Frontend is still a minimal shell, not the full ChartNav platform UI
- Backend still needs real models/routes/integration wiring beyond runtime proof
- No CI workflow has been added yet

## Verification completed
- API: /health returned ok
- Web: returned HTTP 200
- Docker: both api and web containers built and ran successfully

## Recommended next build priority
1. Author first real database migration
2. Replace seed placeholder with demo org/provider/patient data
3. Rebuild frontend shell into actual ChartNav routes/components
4. Add CI for install, typecheck, build, and docker compose config
