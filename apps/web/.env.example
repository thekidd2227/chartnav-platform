# ChartNav web — Vite env.
# All variables must start with VITE_ to be exposed to the client bundle.

# API base URL. Local dev default targets the uvicorn dev server.
VITE_API_URL=http://localhost:8000
