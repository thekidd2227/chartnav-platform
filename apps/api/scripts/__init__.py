"""Operator-driven CLI scripts for the API.

Each module here is meant to be invoked as ``python -m scripts.<name>``
from the api package root. They are intentionally NOT bundled into a
daemon or scheduler — ops decides cadence.
"""
